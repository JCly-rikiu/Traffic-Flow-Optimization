# car.py

from car import Car
from car import CarMap # merge to one line?

def remove(self, number):

if __name__ == '__main__':
    cm = CarMap('test')
    cm.initialCars([(2, 3), (3, 3)])
    tick = 0
    while 
    print(cm.getDirection((4, 4), (8, 4)))
